<%= form_for @genre do |f| %>
  <%= f.label :name %><br>
  <%= f.text_field :name %>

  <br>

  <% if @genre.id.nil? %>
    <%= f.submit 'Create Genre' %>
  <% else %>
    <%= f.submit 'Update Genre' %>
  <% end %>
<% end %>